export PATH="$PATH":/usr/local/bin/

# rbenv
export PATH="$HOME/.rbenv/bin:$PATH"
eval "$(rbenv init -)"

# node
# nodebrewでcurrentを指定しないと、パスエラーになる.
export PATH=$PATH:/Users/NRintaro/.nodebrew/current/bin

# .bashrcの読み込み.
test -r ~/.bashrc && . ~/.bashrc
